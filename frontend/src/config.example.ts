// Copy this file to frontend/src/config.ts and replace the placeholder with your private backend URL.
// Keep `frontend/src/config.ts` out of version control.

export const BACKEND_URL = "https://your.backend.example"; // e.g. https://api.rollcall.example.com

// Example usage note:
// import { BACKEND_URL } from './config';
// axios.get(`${BACKEND_URL}/health`);
